__pycache__/
*.pyc
*.pyo
.DS_Store
.idea/
*.egg-info/
dist/
build/
.env
.venv/
venv/
.cache
.claude
.vscode
*bak

# Local assets (keep repo tidy unless explicitly needed)
assets/gemm_pipeline.png

# Generated example outputs (Ascend header backends)
examples/output_ascend_a2a3/
examples/output_ascend_a5/

# Generated example outputs (per-example output folders)
examples/*/output/
*.so

# Ascend simulator debug dumps
core*_*.dump
profile_*_log*.toml

# Removed `kernels/` in favor of `examples/` flow.
kernels/

# Extracted `ptoas` binaries (generated from the per-platform tarballs).
bin/*/ptoas

# Compiled objects and libraries
*.o
*.a
*.log

# Debug test binaries (keep .c source files)
src/runtime2/tests/debug*
!src/runtime2/tests/debug*.c
src/runtime2/tests/test_*
!src/runtime2/tests/test_*.c
src/runtime2/tests/quick_test

# Trace and benchmark output files in runtime2
src/runtime2/*.json
